# 알고리즘 문제 작업 지침

## 적용 범위와 기본 원칙

- 이 파일은 이 디렉터리와 모든 하위 디렉터리에서 수행하는 알고리즘 문제 작업에 적용한다.
- 사용자가 Markdown 파일 경로를 입력하거나 알고리즘 문제·질문을 일반 프롬프트로 입력하면, 별도의 재확인 없이 아래 절차 전체를 수행한다.
- 모든 설명과 코드 주석은 자연스러운 한국어로 작성한다. 식별자, 언어 키워드, 표준 라이브러리 이름과 채점 사이트가 요구하는 함수 시그니처는 원문 표기를 유지한다.
- 문제의 정답만 제시하지 말고, 학습자가 기본 문법부터 알고리즘 선택 이유까지 이해할 수 있는 독립적인 학습 자료를 만든다.
- 문제에 없는 조건을 임의로 정답처럼 단정하지 않는다. 불완전한 입력 때문에 합리적인 가정이 필요하면 `problem.ko.md`의 "가정" 항목에 명시하고 계속 진행한다. 가정에 따라 문제 자체가 크게 달라져 정답을 결정할 수 없을 때만 사용자에게 질문한다.
- 기존 사용자 파일과 변경 사항을 보존한다. 입력으로 받은 원본 Markdown 파일은 사용자가 명시적으로 요청하지 않는 한 덮어쓰지 않는다.

## 입력 판별 및 작업 위치

### Markdown 파일 경로가 주어진 경우

1. 워크스페이스 기준 상대 경로와 절대 경로를 모두 인식한다.
2. 해당 Markdown 파일을 문제 원문으로 읽고, 그 파일이 있는 디렉터리를 작업 디렉터리로 사용한다.
3. 경로가 디렉터리라면 그 안의 `problem.md`를 우선 사용한다. `problem.md`가 없고 Markdown 파일이 하나뿐이면 그 파일을 사용한다. 후보가 여러 개라 문제 파일을 확정할 수 없을 때만 사용자에게 묻는다.
4. 원문 파일과 아래 산출물 이름이 충돌하면 원문은 그대로 두고 산출물 쪽에 `.generated`를 붙인다. 예: 원문이 `problem.ko.md`이면 번역 결과는 `problem.ko.generated.md`로 만든다.

### 일반 프롬프트가 주어진 경우

1. 프롬프트에서 문제 제목 또는 핵심 주제를 추출해 짧은 영문 `kebab-case` 슬러그를 만든다.
2. 워크스페이스의 `problems/<slug>/` 디렉터리를 생성한다.
3. 같은 이름의 디렉터리에 다른 문제가 이미 있으면 기존 파일을 덮어쓰지 말고 `<slug>-2`, `<slug>-3`처럼 사용하지 않은 번호를 붙인다.
4. 사용자가 입력한 내용을 의미가 바뀌지 않도록 `problem.md`에 먼저 기록한다. 입력이 단순한 알고리즘 질문이라면 질문을 바탕으로 입력, 출력, 제약 조건, 예제를 갖춘 문제 형식으로 정리하되, 새로 정한 조건은 명확히 표시한다.
5. 생성한 `problem.md`를 원문으로 삼아 아래 전체 작업을 이어서 수행한다.

## 필수 산출물

작업 디렉터리에 다음 파일을 만든다.

```text
problem.md          # 사용자가 제공한 원문 또는 일반 프롬프트로 만든 원문
problem.ko.md       # 한국어 문제 번역·정리와 예제 설명
EXPLANATION.md      # 알고리즘 해설, 정확성 증명, 복잡도와 언어별 참고 사항
solution.cpp        # C++20 풀이
Solution.cs         # C#/.NET 풀이
solution.py         # Python 3 풀이
solution.rs         # Rust 2021 풀이
Solution.kt         # Kotlin 풀이
```

- 경로 입력 방식에서는 기존 원문 파일을 `problem.md`로 복사할 필요가 없다. 위 목록의 `problem.md`는 실제 원문 파일로 대체된 것으로 본다.
- 문제에서 특정 파일명, 클래스명, 함수 시그니처 또는 입출력 방식을 요구하면 그것을 우선한다. 다만 산출물 파일명은 가능한 한 위 규칙을 유지한다.
- 여러 언어가 같은 알고리즘, 경계 조건 및 결과를 구현하게 한다. 언어마다 서로 다른 알고리즘을 임의로 사용하지 않는다.

## `problem.ko.md` 작성 규칙

다음 순서로 작성한다.

1. 문제 제목
2. 한국어 문제 설명
3. 입력 형식
4. 출력 형식
5. 제약 조건
6. 예제 입력과 출력
7. 예제에 대한 한국어 설명
8. 필요한 경우 가정 및 주의 사항

- 원문의 의미, 수식, 인덱스 기준, 제약 조건과 예제를 빠뜨리지 않고 한국어로 옮긴다.
- 이미 한국어인 원문도 구조와 표현을 다듬되 의미를 바꾸지 않는다.
- 원문에 입력·출력 형식이 없는 LeetCode형 문제라면 함수 인자와 반환값을 입력·출력 형식으로 설명한다.
- 원문에 없는 예제를 추가했다면 "추가 예제"라고 표시한다.

## 알고리즘 설계 기준

코드를 작성하기 전에 다음을 판단하고 `EXPLANATION.md`에 기록한다.

- 핵심 아이디어와 사용할 자료구조
- 단순한 접근 방법 및 그 방법이 충분하지 않은 이유
- 선택한 알고리즘이 제약 조건에서 안전한 이유
- 단계별 처리 과정
- 경계 조건: 빈 입력, 최소·최대 크기, 중복, 음수, 오버플로, 연결되지 않은 그래프 등 문제에 해당하는 항목
- 정확성 증명: 불변식, 귀납법, 교환 논증, 모순 증명 등 적절한 방식으로 알고리즘이 항상 올바른 답을 내는 이유
- 시간 복잡도와 공간 복잡도. 기호 `n`, `m` 등의 의미도 함께 정의한다.

가능한 해법 중 제약 조건을 만족하는 효율적인 방법을 선택한다. 가독성을 해치지 않는 범위에서 불필요한 복사, 중복 순회, 과도한 메모리 사용과 정수 오버플로를 피한다.

## 공통 코드 작성 규칙

- 다섯 언어 모두 실제로 제출하거나 실행할 수 있는 완전한 풀이를 작성한다. 의사 코드는 허용하지 않는다.
- 채점 플랫폼용 문제라면 요구된 클래스와 함수 시그니처를 정확히 따른다. 표준 입력 문제라면 입력 파서와 출력 코드를 포함한다.
- 문제에 명시되지 않은 외부 패키지에 의존하지 않고 각 언어의 표준 라이브러리를 우선 사용한다.
- 식별자는 역할이 드러나는 이름을 사용하고 의미 없는 축약을 피한다.
- 입력 크기에 맞는 정수형을 사용하고 덧셈·곱셈에서 오버플로 가능성을 점검한다.
- 재귀를 사용할 때는 최대 깊이와 해당 언어의 스택 제한을 검토한다. 위험하면 반복문과 명시적 스택으로 바꾼다.
- 모든 구현에서 동일한 엣지 케이스를 처리한다.
- 생성하는 다섯 언어의 풀이 파일은 루트의 `make run <파일경로>`로 직접 실행할 수 있어야 한다. 표준 입력 문제는 완전한 진입점을 포함하고, 채점 플랫폼의 함수형 문제는 요구된 시그니처를 보존하면서 로컬 실행용 진입점 또는 샘플 하네스를 함께 제공한다.
- 각 문제의 `EXPLANATION.md`에 `make run <파일경로>` 실행 시 기대하는 입력 형식과 실행 예시를 기록한다.

## 줄 단위 교육용 주석 규칙

각 코드 파일은 초보자가 한 줄씩 따라갈 수 있을 정도의 한국어 설명 주석을 반드시 포함한다.

- `import`, `using`, `use`, `#include`부터 변수 선언, 함수 선언, 조건문, 반복문, 자료구조 연산, 반환문까지 모든 의미 있는 코드 줄을 설명한다.
- 주석은 해당 줄의 바로 위 또는 같은 줄 끝에 배치한다. 중괄호만 있는 줄도 그것이 어떤 블록을 시작하거나 끝내는지 설명한다.
- 언어 키워드의 기본 의미와 표준 라이브러리 요소의 역할도 처음 등장할 때 설명한다. 예: `std::vector`가 동적 배열이라는 점, Python의 `dict`가 해시 기반 매핑이라는 점, Rust의 소유권 또는 참조가 이 코드에서 왜 필요한지 설명한다.
- 단순히 코드를 한국어로 읽어 주는 주석에 그치지 말고 "왜 이 연산이 필요한지"와 알고리즘에서의 역할을 설명한다.
- 동일한 동작이 반복되더라도 주석을 생략하지 않는다. 다만 한 줄에 여러 문장을 과도하게 넣지 말고 간결하고 정확하게 쓴다.
- 빈 줄은 주석 대상이 아니다. 자동 포매터가 분리하는 괄호나 문법 토큰은 인접 주석 하나로 묶어 설명할 수 있다.
- 주석 때문에 코드의 동작, 들여쓰기, 채점 사이트 시그니처 또는 컴파일 가능성이 깨져서는 안 된다.

## 언어별 기준

### C++ (`solution.cpp`)

- C++20을 기준으로 작성한다.
- `#include <bits/stdc++.h>` 대신 실제로 사용하는 표준 헤더를 명시한다.
- RAII, `const`, 범위 기반 반복문, 구조적 바인딩, `std::vector`, `std::string`, 표준 알고리즘 등 문제에 적합한 현대 C++ 문법을 사용한다.
- 소유권이 필요하지 않은 큰 객체는 `const T&`로 전달하고, 불필요한 복사를 피한다.
- 인덱스 부호 문제와 정수 오버플로를 검토한다.

### C#/.NET (`Solution.cs`)

- 현재 지원되는 안정적인 .NET/C# 문법을 사용하되 채점 환경의 버전이 명시되면 그 버전에 맞춘다.
- `using` 지시문, 배열·컬렉션, 값 형식과 참조 형식, 제네릭의 역할을 주석으로 설명한다.
- LINQ가 복잡도나 할당을 불필요하게 늘리면 명시적인 반복문을 사용한다.

### Python (`solution.py`)

- Python 3을 기준으로 작성하고 적절한 타입 힌트를 사용한다.
- 리스트, 딕셔너리, 집합, 튜플과 표준 모듈의 역할 및 주요 연산의 비용을 주석으로 설명한다.
- 간결함만을 위한 난해한 한 줄 표현은 피한다.

### Rust (`solution.rs`)

- Rust 2021 edition을 기준으로 작성한다.
- 소유권, 빌림, 가변성, `Option`, `Result`, 컬렉션과 반복자의 사용 이유를 해당 줄 주석으로 설명한다.
- 불필요한 `clone`과 `unsafe`를 피한다. `unsafe`는 문제상 불가피하고 안전성 근거를 설명할 수 있을 때만 사용한다.

### Kotlin (`Solution.kt`)

- 안정적인 Kotlin/JVM 문법을 사용한다.
- `val`과 `var`, nullable 타입, 컬렉션, 범위, 람다의 의미를 해당 줄 주석으로 설명한다.
- 성능이 중요한 입력에서는 적절한 빠른 입력기를 구현하고, 박싱과 불필요한 컬렉션 생성을 피한다.

## `EXPLANATION.md` 작성 형식

다음 항목을 포함한다.

1. 문제 요약
2. 핵심 관찰
3. 알고리즘 선택과 대안 비교
4. 단계별 알고리즘
5. 정확성 증명
6. 시간 복잡도
7. 공간 복잡도
8. 엣지 케이스
9. 언어별 구현 차이와 기본 문법 설명
10. 검증 결과

언어별 파일에 이미 있는 모든 줄 주석을 그대로 반복하지 말고, 언어 간 자료형·컬렉션·입출력·메모리 모델의 차이를 중심으로 설명한다.

## 검증 절차

작업을 마치기 전에 가능한 범위에서 다음을 수행한다.

0. 루트 실행 환경이 준비되지 않았다면 `make env`를 실행한다. 이미 준비된 환경에서는 이 명령이 설치 작업을 반복하지 않는다.

1. 문제의 모든 제공 예제를 다섯 구현에 대입해 결과를 확인한다.
2. 최소 입력, 최대값에 가까운 입력, 중복 또는 빈 입력 등 관련 엣지 케이스를 추가로 확인한다.
3. 사용 가능한 도구 체인이 있으면 각 파일을 실제로 컴파일하거나 문법 검사한다.
4. 독립 실행형 프로그램이면 샘플을 실행한다. 플랫폼 제공 타입이나 테스트 하네스가 필요한 함수형 답안이면 가능한 범위에서 임시 하네스를 사용하되, 제출 파일의 시그니처는 변경하지 않는다.
5. 생성한 임시 바이너리, 캐시와 테스트 파일은 최종 산출물에 남기지 않는다.

기본 검증과 실행은 워크스페이스 루트에서 다음 명령을 사용한다.

```text
환경 구성: make env
C++:      make run problems/<문제>/solution.cpp
C#:       make run problems/<문제>/Solution.cs
C# script: make run problems/<문제>/solution.csx
Python:   make run problems/<문제>/solution.py
Rust:     make run problems/<문제>/solution.rs
Kotlin:   make run problems/<문제>/Solution.kt
```

- `make run`은 확장자를 자동 판별한다. C++, C#, Rust와 Kotlin 결과물은 루트의 `build/` 아래에 원본 상대 경로를 유지해 생성하며, Python은 `uv`가 만든 루트 `.venv`에서 실행하고, `.csx`는 워크스페이스 로컬 `dotnet-script`로 실행한다.
- 여러 파일을 실행할 때는 `make run *.cpp`처럼 glob을 사용할 수 있다. 각 문제 파일은 보통 명시적인 상대 경로로 실행한다.

- 컴파일러가 설치되어 있지 않거나 채점 플랫폼 전용 타입 때문에 로컬 검증이 불가능하면 실패로 숨기지 말고 `EXPLANATION.md`와 최종 응답에 "검증하지 못한 항목과 이유"를 정확히 기록한다.
- 테스트하지 않은 코드를 테스트했다고 표현하지 않는다.
- 컴파일 경고와 오류가 발견되면 수정한 후 다시 검증한다.

## 완료 보고

최종 응답은 간결하게 다음을 알린다.

- 작업 디렉터리
- 생성하거나 수정한 파일
- 선택한 알고리즘과 시간·공간 복잡도
- 언어별 컴파일 및 테스트 결과
- 남아 있는 가정이나 검증하지 못한 항목

파일을 작성하지 않고 채팅 답변만 제공하는 것으로 작업을 끝내지 않는다. 사용자가 특정 언어나 산출물을 명시적으로 제외한 경우에만 해당 파일 생성을 생략한다.
